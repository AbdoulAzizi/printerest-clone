
    {{form_start(form)}}
    {{form_widget(form)}} <br>

    <input type="submit" value="{{ submitButtonText ?? 'Créer Pin' }}" class="w-100 btn btn-primary " formnovalidate>

    {{form_end(form)}}
